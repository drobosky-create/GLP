/**
 * recon.ts — reconstitution math (compounded mode only) + its test suite hooks
 * (formulas and known-good cases from Reference Sheet §2). Pure arithmetic, but a
 * wrong unit count in a health context is the worst possible bug: the feature stays
 * hidden until the /tests suite passes, and the UI shows a persistent
 * "calculator only — verify with your provider" notice (Ref §2.3).
 *
 * Phase 7 (feature-flagged) fills this in.
 */
export {};
